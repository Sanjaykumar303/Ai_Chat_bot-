# Copy this file to ".env" for local overrides, or set VITE_API_URL in
# Vercel's project settings for the deployed build.
#
# Address of the deployed FastAPI backend. Leave unset for local dev -
# src/services/api.js falls back to http://127.0.0.1:8000 automatically.
# VITE_API_URL=https://your-backend.onrender.com
